{
    "builds": [{
        "src": "book_api/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python-3.9.7" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "book_api/wsgi.py"
        }
    ]
}